# 무기 데미지 규칙

- 무기의 직격 데미지는 `stats.damage * stats.numFiring`으로 계산한다. 무기명이나 표시명의 숫자를 발사 수로 추론하지 않는다.
- LBX와 로켓런처는 발당 데미지에 명시적인 발사체 수 `stats.numPerShot`을 곱한다. 로켓런처 10/15/20의 추출값은 각각 1/2/3이며 무기명의 10/15/20을 발사 수로 사용하지 않는다.
- 로켓런처 쿨다운에는 스킬과 쿼크의 전체·미사일·무기별 쿨다운 감소를 적용하지 않는다.
- IS·Clan Streak SRM(SSRM) 2/4/6은 전탄을 동시에 발사하므로 원본 `stats.volleydelay`를 예상 쿨다운과 시뮬레이션 발사 시간에 더하지 않는다.
- 무기 툴팁의 `SHOTS`는 실효 `numFiring`, 실효 `numPerShot`(누락 시 1), 원본 missile `volleysize`(누락 시 1), 실효 `volleydelay`로 계산한다. 순차발사는 `한 이벤트의 발사체 수 X 완전한 이벤트 횟수`로 표시하고 나머지는 ` + 나머지 발사체 수`를 붙인다. 이벤트가 한 번이거나 실효 간격이 0이면 전탄 동시발사로 보고 전체 발사체 수만 표시한다. 예: C-LRM20 `2 X 10`, C-LRM5 `2 X 2 + 1`, C-LRM15 `2 X 7 + 1`, C-SRM6 `6`, C-LB20-X `20`, C-PROTO AC/8 `1 X 2`. 일반 단발 무기는 기존처럼 SHOTS 행을 숨긴다.
- `stats.splashPercent`가 0보다 크면 직격 지점에 인접한 두 부위에 각각 `직격 데미지 * splashPercent`의 스플래쉬 데미지가 적용된다.
- 스플래쉬를 포함한 총데미지는 `직격 데미지 + 스플래쉬 데미지 * 2`로 계산한다.
- 툴팁의 DAMAGE는 `직격 + 스플래쉬 합계` 형식으로 표시하고 스플래쉬 합계 부분만 노란색으로 표시한다. 예: C-ER PPC는 `10 + 5`로 표시한다.
- 데미지 표시는 정수일 때 소수점을 붙이지 않고, 소수가 발생했을 때만 소수 첫째 자리까지 표시한다.
- 머신건·로터리 AC·빔레이저·화염방사기는 연속형 무기다. 이 무기들의 데미지와 발열은 초당값으로 표시하며 DAMAGE와 HEAT에 `/s`를 붙인다.
- 머신건과 로터리 AC의 초당 데미지는 `직격 데미지 * stats.rof`이고 ROF 쿼크도 반영한다. 빔레이저와 화염방사기의 `stats.damage`는 이미 초당 데미지이므로 ROF를 다시 곱하지 않는다. 예: 로터리 AC/5는 `1.1 * 10 = 11/s`다.
- 연속형 무기의 `stats.heat`는 초당 발열이다. HPS와 시뮬레이션에서 ROF 또는 사이클을 다시 곱하거나 나누지 않는다. 현재 IS·Clan 머신건의 추출값은 `stats.heat: 0`이므로 표시값도 `0/s`이고 HPS 기여는 0이다.
- 무기 툴팁의 사거리 그룹은 `MIN RANGE`(최소 사거리가 있을 때만), `OPTIMAL RANGE`, `MAX RANGE`, `VELOCITY` 순서로 표시한다.
- `MIN RANGE`는 첫 구간의 데미지 배율이 최대 배율보다 낮고 이후에 최대 데미지가 시작되는 무기에만 표시한다. 예: LRM은 180m를 표시한다.
- 사거리 증가 쿼크는 `MIN RANGE`에 적용하지 않는다. 시뮬레이션에서도 최소 사거리 경계는 그대로 두고 그 이후의 적정·최대 사거리 경계만 증가시킨다.
- ATM은 60~349m에서 1.25배, 350~649m에서 1배, 650~1100m에서 0.8배의 계단식 데미지를 적용한다. 사거리 쿼크가 있으면 350m 이후의 구간 경계와 최대 사거리만 증가하고 최소 사거리 60m는 유지한다.
- ATM은 특수 스탯 그룹에 `DAMAGE BAND 1~3`을 표시하며 각 행은 `최종데미지 (시작~끝 m)` 형식을 사용한다.
- `stats.spread`가 0보다 크면 사거리 그룹 다음의 별도 그룹에 `SPREAD`를 표시하며, 일반·무기 계열·무기별 퍼짐 감소 쿼크를 반영한다. 원본 `stats.spreadLOS`가 0보다 크면 해당 무기를 LOS 무기로 판정하고 일반 `SPREAD` 그룹과 분리된 다음 특수 그룹에 같은 효과를 반영한 `SPREAD DIRECT`를 표시한다.
- LOS 무기 중 원본 `stats.artemisAmmoType`이 있는 LRM만 다이렉트 탄속 대상이다. `VELOCITY DIRECT`는 원본 `stats.speed * 1.4`로 계산하고 일반 탄속과 같은 탄속 쿼크·장비 효과를 적용하여 다이렉트 특수 그룹의 `SPREAD DIRECT` 다음에 표시한다. ATM과 썬더볼트에는 `VELOCITY DIRECT`를 만들지 않는다.
- `stats.critChanceIncrease`에 0이 아닌 값이 있으면 같은 후속 그룹에 `CRITICAL CHANCE`를 퍼센트로 표시한다. 여러 단계 값은 `14% / 8% / 3%` 형식으로 모두 표시하고, `-1`은 절대 발생하지 않는 단계이므로 빨간색 `X`로 표시한다. 예: 가우스 라이플은 `17% / X / X`로 표시한다.
- `stats.critDamMult`가 존재하고 값이 1이 아니면 `CRITICAL CHANCE` 바로 다음 행에 `CRITICAL DAMAGE`를 배율로 표시한다. 1 미만의 감소 배율은 빨간색으로 표시하고 값이 1이면 행을 표시하지 않는다.
- 화염방사기와 클랜 플라즈마 PPC는 특수 스탯 그룹에 적에게 가하는 발열을 `TARGET HEAT`로 표시한다. 연속형 화염방사기는 `/s` 단위를 사용한다.
- `stats.chargeTime`이 0보다 큰 가우스 라이플 계열은 특수 스탯 그룹에 `CHARGE TIME`을 초 단위로 표시한다.
- 울트라 AC는 `JAM DURATION`, `JAM CHANCE`를 특수 스탯 그룹에 표시하고 관련 쿼크를 반영한다.
- 더블탭 대상은 무기 이름이 아니라 장비 Modifier를 반영한 실효 `ShotsDuringCooldown`으로 판정한다. 값이 `N`이면 한 번의 사격마다 쿨다운 도중 추가 발사를 `N`회 시도하므로 최대 `1 + N`발을 발사한다. 현재 추출 데이터에서는 IS·Clan 울트라 AC 8종만 `1`을 가진다.
- 첫 발은 잼을 굴리지 않는다. 추가 발사는 시도마다 독립적으로 최종 잼 확률을 굴리고, 성공하면 다음 시도로 이어지며 첫 잼에서 남은 시도를 모두 중단한다. 잼 시 추가탄은 발사되지 않으며, 기존 쿨다운은 초기화하지 않고 잼이 발생한 시점부터 잼 지속시간 동안 다음 발사를 막는다.
- 시뮬레이션의 추가 발사는 쿨다운을 멈추지 않으며 `k`번째 시도는 쿨다운 시작 시각 + `(k - 1) * 발사시간`에 발사한다.
- 더블탭 무기의 예상 쿨다운은 기대 발사 수 기준으로 계산한다. 최종 잼 확률을 `p`, 성공 확률을 `s = 1 - p`라고 하면 기대 발사 수는 `1 + s + s^2 + ... + s^N`, 사이클 잼 확률은 `1 - s^N`이다. 식은 `(발사시간 + (1 - 사이클 잼 확률) * 최종 쿨다운 + 사이클 잼 확률 * max(최종 쿨다운, 최종 잼 지속시간)) / max(1, 기대 발사 수)`이며, `N = 1`이면 기존 `max(1, 2 - p)` 식과 같다.
- `ShotsDuringCooldown`은 공용 장비 Modifier 스냅샷의 `shotsDuringCooldown`으로 정규화해 소비한다. SCR-CORLGD의 고정 장비 ID `9035` `UAC Speed Loader`는 IS·Clan 울트라 AC 8종에 `+1`을 더해 시도 수를 2로 만들며, 최대 3발을 발사한다. 세부 규칙은 `장비 Modifier 처리 규칙.md`를 따른다. 이 효과는 장비 자체 툴팁과 대상 무기의 `적용 효과`에 발리스틱 색 `UAC DOUBLE TAP +1`로 표시하고, 무기 스탯 행에는 시도 횟수를 넣지 않는다. 원본 설명의 Targeting Computer MK I 문구는 9032와 공유하는 공용 문구이고 필터에 근거가 없으므로 반영하지 않는다.
- 멕랩 아이템 툴팁은 UI 설정의 `장비 툴팁 적용 효과 표시`가 켜져 있을 때 현재 피팅에서 해당 무기·장비·탄약에 실제로 0이 아닌 효과를 주는 쿼크와 선택 스킬을 `적용 효과`로 표시한다. 설정 기본값은 ON이다.
- 적용 효과 목록은 배리언트·장착 옵니포드·활성 세트 보너스·선택 스킬을 같은 정규화 이름으로 합산하되 이들의 출처는 표시하지 않는다. 무기는 쿨다운, 발열, 듀레이션, 사거리, 탄속, 퍼짐, ROF, AMS 데미지, UAC 재밍, HSL의 실제 계산 판정을 재사용한다.
- 현재 무기 수치를 실제로 바꾸는 장착 장비 효과는 쿼크·스킬 행 다음의 목록 최하단에 장비 표시명을 출처 제목으로 묶어 표시한다. 장비명은 `적용 효과`와 같은 카테고리 제목색을 사용하고 효과명·수치에만 대상 무기의 실제 하드포인트 계열에 해당하는 쿼크 색 규칙을 사용한다. Target Computer 계열은 무기 `name`과 `compatible_weapons`를 정확히 매칭해 첫 번째 `critChanceIncrease`, 사거리, 탄속 중 실제 적용 항목만 `CRITICAL CHANCE`, `RANGE`, `VELOCITY` 순으로 표시한다. Artemis는 선택형 Artemis 무기의 퍼짐 변화만, Railgun Capacitor는 현재 공식의 수량 합산 데미지·발열만 표시한다.
- BANE-L의 고정 장비 ID 9031 `Modified Ballistic Loader`는 멕 이름으로 하드코딩하지 않고 현재 유효 정의의 `fixed` 장비에서 수집한다. 현재 무기 `name`이 `compatible_weapons`에 정확히 일치하는 모든 필터를 원본 배열 순서로 적용한다. 장비 필터의 공통 연산·중첩·소비·확장 규칙은 `장비 Modifier 처리 규칙.md`를 기준으로 한다.
- Modified Ballistic Loader의 HAG는 `spread +0.5`, `numPerShot +3`, `damage ×0.34`, `volleydelay ×0.7`을 적용해 원본 발사 수마다 3 pellet을 발사하며 SHOTS는 HAG20/30/40 순서로 `3 X 4`, `3 X 6`, `3 X 8`이다. Clan Gauss는 `spread +0.25`, `numPerShot +4`, `damage ×0.25`를 적용해 전탄 동시 `SHOTS 4`, pellet당 3.75, 총 피해 15가 된다. C-AC10/20과 C-UAC5/10/20은 필터의 `numFiring` 감소와 `damage` 배율을 적용해 `SHOTS 1`로 합치며 탄약도 1발 소비하고 `SHOT INTERVAL`은 표시하지 않는다. UAC의 Ultra·Jam 수치는 변경하지 않는다. Loader 변환 이후 BANE-L의 기체 쿼크는 별도 계층으로 적용한다.
- 크리티컬 확률의 원본 `-1`은 단계 없음 센티널이므로 Target Computer 또는 Modified 장비의 크리티컬 가산을 적용하지 않고 항상 빨간색 `X`로 유지한다. Modified Ballistic Loader의 `PELLETS / SHOT`, `DAMAGE / PROJECTILE`, `PROJECTILES`와 HAG·Clan Gauss의 `SPREAD`는 실제 계산에는 사용하지만 장비 자체 툴팁과 무기 `적용 효과`에는 표시하지 않는다. HAG의 발사 간격 배율은 두 툴팁에서 `C.HAG INTERVAL ×0.7`로 표시하고, 영향을 받은 무기 스탯의 최종 `SHOT INTERVAL` 값은 초록색으로 표시한다.
- NAGA-AMAROK의 고정 장비 ID 9032 `Modified Missile Loader`는 현재 유효 정의의 `fixed` 장비에서 수집한다. C-LRM 5/10/15/20 일반·Artemis와 C-ATM 3/6/9/12에 exact match하는 모든 필터의 `numFiring`, `ammoPerShot`, `volleydelay`, `cooldown`, `MinReactivationTime`을 원본 순서대로 적용한다. 피해·탄약·쿨다운·발사 이벤트·시뮬레이션은 같은 최종 스냅샷을 소비하고 원본 `volleysize`는 유지한다. 최종 SHOTS는 C-LRM20/15/10/5가 `2 X 4`, `2 X 3`, `2 X 2`, `2`이고 C-ATM12/9/6/3이 `1 X 8`, `1 X 6`, `1 X 4`, `1 X 2`다. `MinReactivationTime`은 최종값만 보존하고 미확정 timing 공식에는 사용하지 않는다. 자체 장비 툴팁에는 `LRM / ATM VOLLEY — STREAM FIRE`, 대상 무기의 장비 효과 그룹에는 `FIRING MODE — STREAM FIRE`만 표시하며 IS LRM 등 필터 밖 무기로 확장하지 않는다.
- AMS와 Laser AMS의 사거리는 공용 `all_range_multiplier`와 일치하는 AMS 전용 사거리 쿼크를 함께 적용한다.
- 비무기는 현재 툴팁 수치에 공식이 연결된 탄약 용량, 실제 히트싱크의 열용량·냉각, 점프젯 연소시간·초기추력, 엔진 최고속도, MASC 최종 기동 효과를 표시한다. ECM 장비는 사용자가 정의한 수신 효과인 `ecmtargetrangereduction_multiplier`와 `stealtharmorcooldown_multiplier`를 표시한다. 탄약 용량 쿼크는 양수와 음수를 부호 그대로 합산해 `floor(기본 발수 + 합산값 × 탄약 톤수)`로 적용한다. 톤수 적용과 내림 후 실제 발수가 바뀌는 경우만 표시하며, 적용 효과 값은 1톤 탄약에 원래 수치를, 0.5톤 탄약에 원래 수치의 절반을 표시한다.
- 로켓런처 쿨다운처럼 공식에서 제외되는 효과, 대상 스탯이 없어 최종 수치를 바꾸지 않는 효과, 점프젯 슬롯·열처럼 개별 툴팁 수치에 소비되지 않는 효과, 일반 센서·Active Probe·Target Computer 센서 보너스처럼 해당 무기 수치를 직접 바꾸지 않는 효과는 목록에 표시하지 않는다.
- 빌드의 알파 데미지는 스플래쉬를 포함한 총데미지를 사용한다.
- 피팅 요약의 연속 알파 발열은 기존 ATO 모델과 같이 초기 열 0에서 시작하고, 알파 간격은 장착 무기의 가장 긴 실효 사이클을 사용한다. 각 사격 직후 열은 `max(0, 이전 열 - 냉각률 × 알파 간격) + 알파 발열`, 점유율은 `사격 직후 열 / 최대 열용량 × 100`이다. 100% 초과도 그대로 표시하며 기본 ALPHA HEAT는 고스트 힛을 제외한다. 고스트 추가열이 0보다 클 때만 별도 GHOST ALPHA HEAT 행에 기본 알파 발열과 공용 고스트 추가열의 합을 같은 연속 알파 공식으로 적용한다. 피팅 ATO와 GHOST ATO는 무장 상세의 공용 ATO 계산을 재사용하며, 각각 기본 발열과 고스트 포함 발열에 같은 최대 열용량·냉각률·최장 실효 사이클을 적용한다. ATO는 열용량 100% 도달 예상 횟수이며 완전히 발사 가능한 정수 횟수로 내림하지 않는다.
- DPS 시뮬레이션은 스플래쉬 적용 토글을 제공하며 기본값은 활성화한다. 비활성화하면 직격 데미지만 계산한다.

## 무장 정보 탄약 규칙

- 탄약 정보 표는 추출된 1톤 일반 탄약만 사용하고 0.5톤과 Artemis 탄약은 제외한다. 톤수·진영 열은 표시하지 않는다. 이름 다음 열은 총 데미지, 탄약 수, HP, 피폭 데미지 순이다. 탄약 수는 `stats.numShots`, HP는 `stats.health`, 피폭 데미지는 원본 `stats.internalDamage`와 `stats.numShots`를 `피폭 데미지 x 탄약 수` 형식으로 함께 표시한다. 예: SRM은 `2.15 x 150`이다. 원본 `stats.internalDamage`가 0이면 `-`로 표시한다.
- 탄약 정보는 모든 탄약, 미사일 탄약, 발리스틱 탄약 표로 구분한다. 분류는 정확히 연결된 무기의 원본 `hardpoint_type` 또는 `stats.type` 계열을 사용하며 이름으로 추론하지 않는다. AMS처럼 두 계열에 속하지 않는 탄약은 모든 탄약 표에만 표시한다.
- 정적 무장 정보에서 무기와 탄약을 연결할 때 현재 피팅의 Artemis 상태를 사용하지 않는다. `JSON 추출 지침.md`가 정의한 정확한 내부 이름 쌍 중 `_Artemis`로 끝나는 별도 무기 레코드는 `stats.artemisAmmoType`, 나머지는 `stats.ammoType`을 정규화한 탄약 `stats.type`과 정확히 매칭한다. `stats.alwaysHasArtemis` 무기는 변환 대상이 아니므로 원본 `stats.ammoType`을 사용한다.
- 탄약 한 통의 총 데미지는 같은 탄종을 쓰는 무기 중 원본 `stats.tons`가 가장 작은 무기를 선택하고, `(stats.numShots / 1회 탄약 소모량) * 해당 무기의 스플래쉬 포함 총데미지`로 계산한다. 이 계산에서는 남은 탄 피해를 버리지 않는다. 거리별 데미지 배율과 피팅 쿼크·장착 Modifier는 적용하지 않는다. 동률은 장비 정렬과 ID로 안정적으로 결정한다.
- 탄약 총 데미지의 대상 무기에 스플래쉬가 있으면 `탄약수 / 1회 소모량`을 직격과 스플래쉬에 각각 곱해 `직격 총합 + 스플래쉬 총합`으로 표시하고 스플래쉬 총합만 노란색으로 표시한다. 일반 탄약 표는 최경량 무기, 공유 탄약 표는 각 행의 무기를 대상으로 같은 표기 규칙을 사용한다.
- 1회 탄약 소모량은 공용 규칙인 `max(1, floor(max(ammoPerShot, numFiring)))`을 사용한다. LB-X의 `numPerShot`은 펠릿 수이며 탄약 소비량에 곱하지 않는다.
- 공유 탄약은 정규화한 활성 탄약 타입에 둘 이상의 무기 레코드가 정확히 연결되는 일반 1톤 탄약이다. 탄종마다 독립 표를 만들며 각 연결 무기의 완전 발사 가능 횟수를 `floor(stats.numShots / 1회 탄약 소모량)`으로 표시한다. 같은 행의 총 데미지는 최경량 무기 기준으로 고정하지 않고 해당 행 무기의 `(stats.numShots / 1회 탄약 소모량) * 스플래쉬 포함 총데미지`를 사용한다.

## 핏팅 브라우저 자동 태그

- 무장과 관련된 자동 태그는 장착 무장을 추출 장비의 정확한 `item id`로 조회하고, 특정 무장 또는 무장 집합을 태그 대상으로 지정할 때도 검증된 정확한 `item id` 목록으로만 판정한다. 무장 `name`, `display_name`, 별칭, 접두·접미사, 이름 유사성 또는 계열명으로 대상 목록을 판정하거나 확장하지 않는다. 전체 무장의 명시적 추출 수치나 계산 결과를 기준으로 하는 태그는 각 장착 `item id`로 조회한 장비 레코드의 해당 필드를 사용한다.
- 사용자가 무장명으로 태그 대상을 지정하면, 구현 전에 현재 추출 장비 데이터에서 대응 후보의 `item id`, 내부 이름, 표시 이름 목록을 작성해 사용자에게 보고한다. 사용자가 그 목록이 실제 적용 대상과 일치한다고 검증·확정한 뒤에만 해당 ID 목록을 규칙, 구현, 테스트에 반영한다. 사용자 확인 전에는 이름에서 추론한 ID 목록을 사실로 확정하지 않는다.
- 자동 태그의 DPS와 알파샷 데미지는 현재 피팅 요약과 같은 쿼크·장착 장비 적용 후 비-AMS 무기 계산값을 사용한다. DPS는 각 무기의 스플래쉬 포함 데미지를 실효 사이클로 나눈 합이며 알파샷은 같은 무기들의 스플래쉬 포함 데미지 합이다.
- `고화력`은 전체 DPS가 20 이상, `쿨러`는 현재 공통 열효율이 80% 이상일 때 표시한다.
- 사거리 타입은 무기의 원본 최대 데미지 사거리로 분류한다. 350m 이하는 근거리, 350m 초과 700m 이하는 중거리, 700m 초과는 장거리다. 해당 타입의 합산 DPS가 5를 초과하면서 전체 DPS의 40% 이상이면 각각 `근거리`, `중거리`, `장거리` 태그를 표시한다.
- `스나이퍼` 대상은 사용자가 검증한 정확한 무장 `item id`인 ER 라지 레이저 `1005`, `1213`, ER PPC `1006`, `1217`, 가우스 라이플 `1021`, `1208`, Silver Bullet Gauss `1079`, HAG 20·30·40 `1255`, `1256`, `1257`이다. 이 ID에 해당하는 무기들의 합산 알파샷 데미지가 20 이상이면서 전체 알파샷 데미지의 40% 이상일 때 표시한다.
- `브롤러`는 위 사거리 타입에서 근거리로 분류된 무기들의 합산 알파샷 데미지가 30 이상이면서 전체 알파샷 데미지의 70% 이상일 때 표시한다.

## 멕랩 장비 목록 필터

- 선택한 멕의 실효 하드포인트에 없는 종류의 무기는 멕랩 장비 목록에 표시하지 않는다.
- 일반 멕은 기본 스펙의 하드포인트를 사용하고, 옴니멕은 현재 장착한 옴니포드의 하드포인트만 사용한다. 하드포인트가 없는 옴니포드에 기본 포드의 하드포인트를 대신 적용하지 않는다.
- 에너지, 미사일, 발리스틱, AMS 무기는 각각 대응하는 실효 하드포인트가 하나 이상 있을 때만 표시한다.
- ECM은 일반 멕의 `CanEquipECM` 스펙 또는 ECM 하드포인트가 있을 때 표시한다. 옴니멕은 현재 옴니포드 구성에 ECM 하드포인트가 있을 때만 표시한다.
- 점프젯은 `MaxJumpJets`가 0보다 크고 해당 점프젯의 중량 범위와 멕 중량이 맞을 때만 표시한다.
- MASC는 `CanEquipMASC` 또는 `CanEquipMasc`가 활성화되어 있고 해당 MASC의 중량 범위와 멕 중량이 맞을 때만 표시한다.
