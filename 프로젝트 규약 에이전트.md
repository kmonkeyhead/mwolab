# 프로젝트 UI·표시 규약

이 문서는 MwoLab의 UI 처리 원칙, 탭별 표시 규격, 슬롯과 하드포인트 표시 규칙을 정의한다.
제품의 목적, 제공 기능, 대상 사용자와 범위는 `프로젝트 사양서.md`를 기준으로 한다.

## 1. 문서 범위

- 이 문서에는 여러 UI 변경에서 재사용할 표시·상태·레이아웃 규칙을 기록한다.
- 무기 계산은 `무기 관련 에이전트.md`, 쿼크 계산은 `AGENTS.md`, 데이터 생성은 `JSON 추출 지침.md`를 따른다.
- 현재 코드의 함수 위치나 일회성 구현 설명은 이 문서에 복사하지 않고 `wiki/mwolab-implementation-wiki.html`에 기록한다.

## 2. 공통 UI 규칙

### 표시 원칙

- 조용하고 밀도 높은 도구형 화면을 유지한다. 장식보다 스캔, 비교, 반복 사용을 우선한다.
- UI는 한국어(`kr`, 기본)와 영어(`en`)를 지원한다. 지원 언어 판정은 `public/app.js`의 `SUPPORTED_LANGUAGES`를 기준으로 한다.
- 새 사용자 텍스트는 `data-i18n` 계열 속성 또는 공통 번역 키를 사용하고 한국어와 영어를 함께 채운다.
- 게임 원본명, 장비명, 스탯 키, 진영명처럼 원 데이터의 고유 표기는 영어 값을 유지한다.
- 공통 수치는 탭마다 별도 공식을 만들지 않고 같은 계산 경로에서 파생한다.
- 쿼크 적용 여부와 기본값 대비 강조가 필요한 수치는 정보, 비교, 통계에서 같은 기준을 사용한다.

### 스크롤 영역의 공간과 표시

- 모든 스크롤 뷰는 최초 배치부터 콘텐츠 영역과 스크롤바용 여백을 분리한다. 내용이 짧아 스크롤이 필요 없을 때도 같은 여백을 확보한다.
- 스크롤이 필요 없으면 스크롤바·트랙·버튼을 표시하지 않고 빈 여백만 남긴다. 스크롤이 필요해지면 이미 확보한 여백 안에 스크롤 손잡이를 표시한다.
- 스크롤바가 나타나거나 사라져도 콘텐츠의 가로 폭, 열 위치, 텍스트 줄바꿈이 변하지 않아야 한다. 패널 바깥 크기만 같고 내부 콘텐츠가 압축되는 구현은 허용하지 않는다.
- 여백 확보를 위해 스크롤바를 항상 보이게 하거나, 스크롤이 발생한 뒤 콘텐츠 폭을 줄이거나, 스크롤바로 콘텐츠를 가리는 방식은 사용하지 않는다.
- 이 규칙은 PC·모바일의 문서, 정보패널, 목록, 표, 팝업 등 모든 스크롤 뷰와 새로 추가하는 스크롤 뷰에 적용한다. 단순 잘림·말줄임 요소에는 불필요한 스크롤바 여백을 추가하지 않는다.
- 구현 수단보다 위 표시 결과를 기준으로 판단한다. 현재 CSS 적용 방식은 `wiki/mwolab-implementation-wiki.html`을 참조한다.

### 개인설정

- 기존 UI 버튼은 `개인설정`으로 표시하고 상단 프로필·로그인 묶음 밖의 바로 왼쪽에 독립 항목으로 둔다. 설정 창은 탭을 바꿔도 크기가 달라지지 않는 고정 크기로 유지하고 `로드아웃`, `수치`, `공용` 탭으로 나눈다. 로그인 없이 브라우저의 localStorage에 저장해 브라우저 종료·재실행 후에도 복원한다.
- 데스크톱에서 멕을 새로 선택할 때 스톡 로드아웃 또는 장비 제거(기본)를 적용한다. 새 `mech` URL 초기화도 같은 설정을 사용하되 기존 탭·히스토리 스냅샷, IMPORT, 공유·추천 및 저장된 핏팅은 원본을 유지한다. 정보·비교·통계의 스톡 기준 계산과 명시적 STOCK LOADOUT 작업, 모바일 초기화에는 적용하지 않는다.
- 장비 제거는 기존 STRIP EQUIPMENT와 같이 제거 가능한 장비 및 엔진 히트싱크 배열만 비우며 고정 장비·옴니포드·업그레이드는 유지한다.
- 로드아웃 방식과 아머 설정은 중첩된 패널 상자로 감싸지 않고 제목·구분선으로 나눈다. 선택지는 기본 라디오 원형을 노출하지 않는 동일 크기 세그먼트 버튼으로 표시한다. 아머 설정은 항상 표시하되 `장비 제거`일 때만 활성화한다. `풀아머`(기본), `아머 없음`, `커스텀`을 선택하며, 커스텀 수치도 항상 표시하되 장비 제거와 커스텀이 함께 선택됐을 때만 활성화한다. 아머 없음은 모든 전면·후면 아머를 0으로 배정한다. 커스텀은 저장된 부위별 비율을 사용하며 다른 모드로 바꿔도 비율을 보존한다. 풀아머는 기존 MAX ARMOR처럼 스톡 몸통 후면을 유지하고 남은 용량을 전면에 배정한다.
- 커스텀 아머 수치는 머리, 측면 몸통, 몸통, 팔, 다리 순서의 다섯 그룹만 제공한다. 좌우 측면 몸통·팔·다리는 같은 비율을 사용한다. 측면 몸통과 몸통의 전후면은 같은 한도를 공유하고 독립 입력하며 합계 100%를 초과하는 입력은 상대편 비율을 제외한 최댓값으로 제한한다. 비율은 쿼크·스킬 적용 전 장착 한도 기준 0~100 정수 %이며 실제 아머 배정은 `floor(부위 장착 한도 * 설정 비율 / 100)`이다. 나머지는 배정하지 않고, 설정 변경은 다음 새 피팅에 적용하며 열린 빌드를 변경하지 않는다.

### 선택 상태

- 언어 전환은 현재 문서를 다시 로드하지 않고 적용한다. 현재 주 탭, 탭별 멕 선택, 멕랩 피팅 탭과 편집 중인 빌드, 비교·통계 선택 및 현재 URL의 다른 쿼리·해시를 유지하고 `lang`만 바꾼다.
- 멕랩과 정보 탭은 각각의 마지막 선택 멕을 유지한다. 탭별 선택은 `state.selectedMechIdsByTab`을 기준으로 복원한다.
- 멕랩 피팅은 `state.mechlabTabs`의 탭별 멕·빌드로 유지하고 `state.activeMechlabTabId`가 현재 채워진 피팅을 정한다. `state.mechlabPendingTabIndex`가 있으면 마지막 `+` 빈 슬롯이 현재 포커스이며 다음 피팅 결과가 우선 할당된다. `state.selectedMech`와 `state.currentBuild`는 마지막 활성 피팅 탭의 작업용 참조다.
- 비교 대상은 `state.compareMechIds`, 통계 행 선택은 `state.selectedStatsMechId`로 별도 관리한다.
- 무장 정보 탭은 선택 멕에 의존하지 않는다.

### 전체 레이아웃과 멕 브라우저

- 상단은 프로젝트명, 데이터 상태, 멕랩 요약 스트립, 메인 탭, 후원, 도움말, 언어 전환을 제공한다.
- 요약 스트립은 멕랩 빌드 상태를 보여주며 멕랩이 아닐 때 숨긴다.
- 멕랩·정보·비교하기는 같은 멕 브라우저 영역을 사용하지만 각 탭의 선택 의미는 위 선택 상태 규칙을 따른다.
- 무장 정보와 통계는 멕 브라우저를 숨기고 전체 폭을 사용한다.
- 멕 브라우저는 검색, 필터, 큰/작은 목록, 기본·톤수·알파벳순 정렬, 오름·내림차순을 제공한다.
- 쿼크 필터의 각 행은 왼쪽 쿼크 선택 슬롯과 오른쪽 선택적 수치 입력 슬롯을 한 줄에 표시한다. 수치가 비어 있으면 해당 쿼크의 보유 여부만 판정하고, 수치를 입력하면 쿼크 선택도 함께 활성화한다.
- 쿼크 필터 입력은 `_multiplier` 값을 퍼센트 단위로, `_additive` 값을 원본 수치 단위로 받는다. 입력값은 0 이상의 소수를 허용하며, 멕의 동일 이름 유효 쿼크 합산값을 표시 단위로 변환한 절댓값이 입력값 이상인지 판정한다.
- 쿼크 선택을 해제하면 그 행의 입력값도 지우고, 입력값만 지우면 선택은 유지해 보유 여부 필터로 돌아간다. 숫자 입력의 마우스 휠 변경은 막는다.
- 기본 필터의 각 하드포인트 타입은 전체 최솟값을 1로 초기화하고 1 미만으로 내리지 않는다. 개별 부위 최솟값은 0을 허용한다.
- 멕 브라우저의 개별 배리언트 행과 카드는 추출된 영문 현지화 `display_name`을 표시하고, 해당 값이 없을 때만 원본 배리언트 코드를 대체값으로 사용한다.
- 정렬 버튼으로 여는 창은 다른 모달 팝업과 마찬가지로 화면 가운데에 여유 있는 크기로 표시하고 배경을 어둡게 처리하며 닫기 버튼을 제공한다. 진영별 표시는 스위치형 토글로 제공한다.
- 체급 구분은 항상 유지한다. 진영별 그룹 표시는 기본으로 켜며 사용자가 끄면 같은 체급 안에서 양 진영 기종을 함께 정렬한다.

## 3. 탭 표시 규격

탭 표시 순서는 `멕랩 · 무장 정보 · 정보 · 비교하기 · 통계`이며 멕랩이 기본 활성 탭이다.

### 멕랩 탭

- 멕을 선택하기 전에는 멕 브라우저를 주 작업 영역으로 사용하고, 선택 후에는 피팅 작업 영역으로 진입한다.
- 피팅 헤더에는 최대 10개의 세션 전용 피팅 탭과 마지막 `+` 버튼을 표시한다. 각 탭은 멕과 빌드를 독립적으로 보존하며 같은 멕을 중복해서 열 수 있다. 새로고침이나 재접속 후에는 복원하지 않는다.
- `+`는 마지막의 비어 있는 탭 슬롯이며 누르는 즉시 현재 포커스 인덱스가 된다. 빈 슬롯 포커스는 다른 주 화면을 다녀와도 유지하고, 이후 전체 멕 목록, 정보·통계의 `핏팅하기`, MWO 코드 IMPORT 중 먼저 확정된 피팅을 그 슬롯에 새 탭으로 할당한다. 기존 피팅 탭을 직접 선택한 뒤의 같은 동작은 선택 탭을 교체한다. 전체 목록의 미리보기 선택만으로 포커스나 기존 피팅을 바꾸지 않는다.
- 멕랩 전체 목록의 IMPORT 옆에는 채워진 피팅 탭이 하나 이상 있을 때만 `RETURN` 버튼을 표시한다. 누르면 `+` 빈 슬롯 포커스를 취소하고 마지막으로 활성화했던 피팅 탭으로 즉시 돌아간다.
- 피팅 화면의 `멕 리스트`로 여는 컴팩트 목록은 각 변형 슬롯 오른쪽에 여백과 테두리를 가진 독립적인 새 탭 추가 `+` 버튼을 표시한다. `+`는 상단 피팅 탭의 추가와 동일하며 10개 한도에서는 비활성화한다. 슬롯 본문 클릭은 기존 선택 동작을 유지한다. 컴팩트 슬롯의 하드포인트 태그는 한 줄에 최대 4개씩 배치하고 초과분은 다음 줄에 표시한다.
- 활성 탭을 닫으면 바로 왼쪽 탭으로 이동하고 첫 탭을 닫았으면 새 첫 탭을 연다. 마지막 한 탭에는 닫기 버튼을 표시하지 않으며 10개일 때 `+`는 비활성화한다.
- 피팅 작업 영역은 멕 요약, 컴포넌트, 장비 목록, 업그레이드와 작업 버튼을 제공한다.
- 멕 요약은 톤수·슬롯 한도, 기동성, 장갑·스트럭쳐, 센서·점프젯, 열, 무장 성능, 탄약, 스킬·쿼크를 묶어 표시한다.
- 피팅 왼쪽 알파 발열은 WEAPON 다음의 독립 `ALPHA HEAT` 그룹에 둔다. 그룹의 네 수치 열 위에는 `1ST`, `2ND`, `3RD`, `4TH` 머리글을 표시한다. 기본 ATO를 추가하고 고스트 힛 발생 시 GHOST ATO도 표시한다. ATO는 기존 무장 상세처럼 소수 둘째 자리까지 표시하고 계산 불가 시 `-`, 열이 누적되지 않을 때 `∞`로 표시한다. 각 항목과 차수 머리글에는 짧은 한국어·영어 마우스 안내 툴팁을 제공한다. `ALPHA HEAT`는 라벨 오른쪽의 동일 너비 네 칸에 1~4차 알파샷 직후의 열용량 점유율을 순서대로 표시한다. 퍼센트는 정수로 반올림하며 칸은 테두리로 구분하고 `|` 문자를 출력하지 않는다. 고스트 힛이 발생하면 바로 아래 `GHOST ALPHA HEAT` 행에 고스트 포함 열점유율을 표시하고, 두 행의 라벨과 네 수치 칸은 같은 열 위치를 사용한다. 두 행의 글자색은 반올림 전 점유율 기준 25% 이하 초록, 50% 이하 연두, 75% 이하 노랑, 100% 이하 주황, 100% 초과 빨강의 5단계다. 연속 알파 계산은 `무기 관련 에이전트.md`를 따른다.
- 컴포넌트는 장갑 배분, 스트럭쳐, 남은 하드포인트, 크리티컬 슬롯과 장착 아이템을 표시한다.
- 장비 목록은 무기, 탄약, 장비, 옴니포드 카테고리를 사용하며 선택 멕에 장착할 수 없는 항목을 필터링한다.
- 도구 메뉴는 장갑·장비 초기화, 최대 장갑, 스톡 로드아웃을 제공한다.
- 시뮬레이션, 스킬 적용, MWO 코드 입출력, URL 공유, 로컬 저장·불러오기를 제공한다. 신규 로드아웃 공유 URL은 <code>loadout</code> 쿼리에 <code>z</code> 접두사의 DEFLATE·Base64URL 가역 압축값을 담고, 기존 전체 MWO 코드를 담은 <code>loadout</code> URL도 읽기 호환한다.
- 상단 요약 스트립은 Tons, Alpha, Heat, Ammo, Engine, Slots, Status를 표시한다.

### 모바일 전용 멕랩

- 휴대폰과 태블릿은 기본 주소에서 별도 모바일 경로로 항상 자동 이동한다. PC 보기 선택이나 저장된 보기 선호로 이 자동 이동을 우회하지 않는다.
- 모바일은 멕랩만 제공하고 로그인, 통합 핏팅 브라우저, 정보·비교·통계·무장 정보, 시뮬레이션과 스킬 적용 진입점을 표시하지 않는다. Firebase 로그인 모듈과 스킬 데이터는 모바일에서 로드하지 않는다. 단, `fitting` 공유 문서 URL로 직접 진입하면 공개 Firestore REST API로 해당 문서 한 건의 `loadoutCode`만 읽어 공용 IMPORT 경로로 적용한다. 로딩·누락·형식 오류·조회 실패는 멕 목록 상단의 보이는 상태 영역에 표시하고, 뒤로가기로 `fitting` URL에 복귀하면 다시 조회한다.
- 멕 목록의 검색 입력은 목록을 스크롤해도 상단에 고정한다. 체급 아래에는 공용 순서와 명칭을 사용한 진영 구분을 표시하고, 각 진영 아래에 기종 카테고리를 접힌 행으로 둔다. 카테고리를 누르면 그 기종의 배리언트 소형 행을 펼치며, 다른 기종을 열어도 기존 기종의 펼침 상태를 유지한다. 배리언트를 한 번 눌러 단일 피팅 화면으로 진입한다. 검색 중에는 일치한 배리언트가 속한 체급·진영·기종을 함께 표시하고 기종을 펼친다. 배리언트 행에는 진영명을 중복 표시하지 않으며 PC 작은 멕 리스트의 공용 슬롯 배지를 그대로 사용해 하드포인트 수량, `ECM`, `JJ`, `MASC`와 옵니포드 아이콘을 표시한다.
- 피팅 컴포넌트는 게임과 가까운 5열 배치를 하나의 캔버스로 유지하며 한 손가락 이동과 두 손가락 확대·축소를 지원한다. 모바일 각 부위 열은 최소 14rem, 전체 캔버스는 72.2rem 폭을 사용해 PC보다 가로 공간을 넓게 확보한다.
- 모바일 앱 루트와 피팅 작업영역은 동적 뷰포트 높이를 사용하고, 숨긴 PC 상단 행의 그리드 공간을 남기지 않는다. 피팅 진입 시 캔버스의 초기 배율은 하단 상태줄 위까지 세로 공간을 사용하도록 높이에 맞추고 중앙 몸통을 화면 가운데 둔다. 가로로 벗어난 좌우 부위는 한 손가락 이동으로 확인한다.
- 이동식 장비 슬롯은 더블탭으로 제거한다. 엔진 내부 히트싱크는 히트싱크 레이아웃 아래의 `-`·`+` 버튼으로 사용자 장착분을 한 개씩 제거·추가하고, `+`는 현재 히트싱크 업그레이드의 명시적 `compatibleHeatSink`만 사용한다. 고정 엔진 히트싱크와 고정·내장·업그레이드·엔진 슬롯은 제거 대상이 아니며 장비 드래그는 지원하지 않는다.
- 현재 부위에서 위치가 확정되지 않은 구조·장갑 업그레이드 슬롯은 일반 빈 슬롯처럼 장비 선택 목록을 연다. 계산 결과에서 점유가 확정된 구조·장갑 업그레이드 슬롯은 선택 동작을 제공하지 않는다.
- 배틀멕은 중앙 몸통의 빈 슬롯으로 연 장비 탭에서도 교체 가능한 엔진을 표시한다. 기존 엔진 선택으로 여는 전용 목록과 같은 진영·엔진 등급 제한 및 교체 검증을 적용하며 옴니멕에는 추가하지 않는다.
- 빈 슬롯을 누르면 해당 부위에 장착할 항목 목록을 전체 화면으로 연다. 무기 카테고리는 현재 남은 수량이 아니라 그 부위의 원래 하드포인트 계열을 기준으로 E·M·B·AMS 그룹 제목을 유지하고, 남은 수량이 0이면 `B 0`, `M 0`처럼 표시한다. 하드포인트를 모두 사용해도 해당 계열 무기 목록은 숨기지 않고 공용 장착 검증 경고에 따라 빨간색으로 표시한다. 그룹 제목, 남은 수량과 각 무기 행에는 PC와 같은 공용 계열 색을 적용한다.
- 모바일 무장 선택 목록 상단에는 선택 부위의 남은 하드포인트, 공용 계산 결과의 `현재 톤수 / 최대 톤수`, `현재 슬롯 / 최대 슬롯`을 한 줄로 표시한다. 톤수나 슬롯 한도를 초과하면 해당 요약값을 빨간색으로 표시한다.
- 모바일 무장 선택 목록을 스크롤해도 카테고리 탭과 남은 하드포인트·톤수·슬롯 요약 및 현재 상태 메시지는 상단에 고정한다.
- 모바일 무장 선택 목록은 마지막으로 본 무기·탄약·장비·옵니포드 탭을 페이지 세션 동안 기억해 다음에 목록을 열 때 먼저 표시한다. 다음 부위에 기억한 탭이 없으면 무기 탭으로 대체하며 엔진 전용 목록은 이 상태를 변경하지 않는다.
- 모바일 무장 선택 목록의 각 항목은 PC 기준 슬롯 높이의 2배를 사용하고, 피팅 컴포넌트의 고정·장착·빈 슬롯 행은 PC 기준 높이의 1.5배를 사용한다. 여러 슬롯을 점유하는 장비의 행 구분 배경도 같은 배율을 따른다.
- 슬롯이나 다른 장착 조건이 부족한 후보는 빨간색으로 표시하고 선택해도 장착하지 않는다. 기존에 초과된 부위도 공용 빌드 경고로 빨간색 표시한다.
- 장비 후보를 한 번 누르면 공용 장착 검증을 통과한 경우 선택 부위에 바로 장착한다.
- 옵니포드 카드는 같은 부위의 옵니포드 목록을 열고 중앙 몸통 고정 포드는 교체하지 않는다.
- 엔진 슬롯은 제거하지 않고 중앙 몸통용 엔진 목록을 연다. 고정 옴니멕 엔진은 교체 불가 안내를 표시한다.
- 아머 조절은 수치의 왼쪽에 `-`, 오른쪽에 `+`를 배치하고 기존 전·후면 배분 한도와 길게 누르기 동작을 유지한다.
- 하단 메뉴는 오버뷰, 툴즈, 업그레이드, 저장/불러오기 순서다. 각 기능은 화면 전체를 가리는 페이지로 열고 같은 형태의 하단 `닫기` 버튼을 제공한다. 툴즈에는 헤더 X나 별도 영문 CLOSE 버튼을 중복 표시하지 않는다.
- 피팅 화면에는 하단 메뉴 바로 위에 공용 계산 결과의 `현재 톤수 / 최대 톤수`와 `현재 사용 슬롯 / 전체 슬롯`을 고정 표시하고, 한도 초과 항목은 빨간색으로 표시한다.
- 오버뷰는 PC 멕랩 왼쪽 요약과 같은 공용 계산 결과를 표시한다. 툴즈는 기존 장갑·장비 초기화, 최대 장갑과 스톡 복원 동작을 재사용한다.
- 업그레이드는 공용 업그레이드 선택과 함께 `현재 사용 슬롯 / 전체 슬롯`, `남은 슬롯`을 표시한다. 업그레이드 대상을 누르면 즉시 빌드에 반영하고 같은 화면의 현재·남은 슬롯 수를 다시 계산해 바로 갱신한다.
- 저장/불러오기는 MWO 코드 IMPORT와 EXPORT만 제공한다. 선택 버튼은 같은 너비의 두 열로 정렬한다.
- 모바일 EXPORT 페이지의 하단 작업줄은 `URL 복사`, `코드 복사`, `X`를 같은 너비로 표시하며 헤더의 중복 닫기 버튼은 숨긴다.
- 모바일에서 생성한 공유 URL은 `/mobile/` 경로를 포함하지 않고 기본 사이트 경로를 사용해 접속 기기에 따른 자동 분기가 다시 적용되게 한다.
- 모바일에서는 장비·옵니포드·고스트 힛 툴팁을 열지 않는다.
- 모바일 일반 장비 후보는 PC 장비 창고와 같은 선택 가능 섹션 판정을 사용한다. 추출 데이터의 `equipment` 패밀리에 포함됐더라도 고정·내부 효과 전용이라 PC 창고에서 제외되는 모듈은 모바일에서도 장착 후보로 표시하지 않는다.
- 왼쪽 위 3선 메뉴는 멕 목록, 한국어·영어 전환과 후원 링크를 제공하며 PC판으로 보기 항목은 표시하지 않는다.
- 모바일 오버뷰에는 모바일에서 지원하지 않는 무장 상세 `자세히` 버튼을 표시하지 않는다.
- 모바일 툴즈의 하단 닫기 버튼은 작업 목록과 같은 좌우 기준으로 전체 너비를 사용하고 safe area를 한 번만 적용해 다른 모바일 오버레이의 하단 닫기 동작과 정렬을 맞춘다.

### 무장 정보 탭

- 선택 멕과 무관하게 장비 자체를 조회하며 멕 브라우저를 사용하지 않는다.
- 보기는 무기, 탄약, 모듈, 고스트 힛 네 종류다.
- 무기 보기에는 원본 `stats.spreadLOS`가 있는 장비만 모은 `LOS 무기` 표를 추가하고, 이름·탄 퍼짐·탄 퍼짐 다이렉트·탄속·탄속 다이렉트를 표시한다. 개별 무기에 값이 없는 다이렉트 탄속은 `-`로 표시하며 대상과 계산 공식은 `무기 관련 에이전트.md`를 따른다.
- 탄약 보기는 Artemis와 0.5톤을 제외한 1톤 원본 탄약만 사용한다. 톤수·진영 열 없이 이름 다음에 총 데미지·탄약 수·HP·피폭 데미지 순으로 제공하며, 피폭 데미지는 `원본 피폭 데미지 x 탄약 수` 형식으로 표시하고 원본값이 0이면 `-`로 표시한다. 같은 행 집합을 모든 탄약·미사일 탄약·발리스틱 탄약 표로 구분한다. 일반 탄약 표는 열의 실제 자연 폭만 사용해 오른쪽 빈 공간을 만들지 않고 왼쪽에 배치한다. 공유 탄약은 탄종마다 동일한 고정 폭의 왼쪽 정렬 독립 표를 만들고 연결되는 각 무기, 완전히 발사 가능한 횟수와 해당 무기로 탄약 전체를 소비할 때의 총 데미지를 표시한다. 탄약-무기 매칭과 계산 공식은 `무기 관련 에이전트.md`를 따른다.
- 미사일 탄약과 발리스틱 탄약 표의 카테고리 제목 및 왼쪽 강조선은 무기 보기의 미사일·발리스틱 계열색을 그대로 사용한다.
- 탄약 표의 총 데미지는 해당 무기에 스플래쉬가 있으면 무기 DAMAGE 표기와 같이 `직격 총합 + 스플래쉬 총합`으로 나누고 스플래쉬 부분만 노란색으로 표시한다. 이 규칙은 일반 탄약 표와 공유 탄약 표에 모두 적용한다.

### 정보 탭

- 헤더에는 선택 멕 이름과 `진영 - 체급 - 톤수`를 표시하고 해당 멕을 멕랩 피팅으로 넘기는 버튼을 제공한다.
- 쿼크 적용 토글을 제공한다.
- 정보 카드는 다음 순서를 사용한다.

1. 쿼크 서머리: 최상단 독립 카드
2. 종합 내구: 아머와 스트럭쳐 총합 및 부위별 수치
3. 기동성: 최대·후진 속도, 가속, 감속, 선회 속도, 회전각 X/Y, 몸통 회전속도
4. 스트럭쳐 정보: 총합과 부위별 수치
5. 아머 정보: 최대 아머 총합과 부위별 수치
6. 엔진: 최소·최대 엔진 레이팅
7. 쿼크 리스트: 쿼크명, 출처, 값

### 비교하기 탭

- 비교 탭 진입 시 비교 모드를 사용하고 왼쪽 멕 목록에서 최대 15개를 선택한다.
- 헤더는 선택 개수와 선택된 배리언트 코드를 표시한다.
- 비교 리스트 모두 지우기, 차이 표시, 쿼크 적용 컨트롤을 제공한다.
- 각 멕 열은 기준 선택, 배리언트 코드, 제거 버튼, 진영과 톤수를 표시한다.
- 비교 그룹은 기본 정보, 내구도 요약, 기동성, 종합 내구, 아머 정보, 스트럭쳐 정보, 쿼크 순서다.
- 종합 내구, 아머 정보, 스트럭쳐 정보는 기본으로 접는다.
- 기준 멕을 지정하면 차이를 기준 멕 대비로 계산하고 상승·하락을 강조한다.
- 테이블 헤더가 스크롤 밖으로 나가면 고정 오버레이 헤더를 표시한다.

### 통계 탭

- 멕 브라우저를 숨기고 전체 폭 목록과 오른쪽 상세 패널을 사용한다.
- 순위 방식은 개별과 기종별이다. 기종별은 평균, 최대치, 최소치 집계를 선택한다.
- 통계 종류와 세부 범위는 다음과 같다.

| 종류 | 카테고리 | 세부 범위 |
|---|---|---|
| 내구도 | 총합, 아머, 스트럭쳐 | 전체, 어깨+몸통, 몸통, 어깨, 머리, 중앙 몸통, 좌·우 몸통, 좌·우 팔, 좌·우 다리 |
| 기동성 | 가속, 감속, 선회 속도, 몸통 회전속도 | 별도 범위 없음 |
| QUIRKS 쿨다운 | 쿨 다운 | 전체, ENERGY, MISSILE, BALLISTIC, ENERGY 듀레이션, RAC ROF, MG ROF, 잼찬스, 잼 듀레이션 |
| QUIRKS 발열 | 발열 | 전체, ENERGY, MISSILE, BALLISTIC, HEAT DISSIPATION |
| QUIRKS 내구도 | 내구도 | 전체, 아머, 스트럭쳐, 크리 방지 |
| QUIRKS 사거리 | 사거리 | 전체, ENERGY, MISSILE, BALLISTIC, 추가 센서 |
| QUIRKS 탄속 | 탄속 | 전체, ENERGY, MISSILE, BALLISTIC |

- 목록 모드는 전체 목록과 필터이며 필터는 진영과 체급 또는 톤수를 기준으로 한다.
- 쿼크 통계에서는 값이 없는 멕을 숨기는 옵션을 제공한다.
- 각 행은 순위, 멕 또는 기종 이름, 진영·톤수, 선택 수치, 체급 배지와 하드포인트·JJ·MASC 배지를 표시한다.
- 개별 행 상세는 선택 멕 정보와 멕랩 피팅 버튼을 제공한다.
- 기종별 상세는 평균·최대·최소와 대표 정보를 보여주며 특정 배리언트의 피팅 버튼은 제공하지 않는다.
- 표시할 행이 없거나 선택된 행이 없으면 해당 빈 상태를 표시한다.

### 공개·로컬 핏팅 브라우저

- 핏팅 브라우저는 공개, 로컬, 내가 올린 핏팅의 세 탭을 한 대화상자에서 제공한다.
- 핏팅 브라우저의 대화상자는 목록 개수와 무관하게 데스크톱에서 최대 64rem 높이로 고정한다. 목록은 자체 스크롤하고 페이지당 최대 25개를 표시하며 숫자 페이지는 현재 구간부터 최대 5개씩 묶어 표시한다. 100개일 때는 1~4페이지를 즉시 표시한다. 상세 열 전체에 별도 스크롤을 만들지 않고 헤더와 하단 작업 버튼 사이의 본문만 스크롤한다.
- 상세 정보는 아머 비율, 톤수, 엔진, 최대 속도, DPS, 알파샷 데미지, 열 효율, 히트싱크 수 순서로 표시한다. 아머 비율은 현재 아머를 최대 아머로 나눈 값이며 `0~20`, `20 초과~40`, `40 초과~60`, `60 초과~80`, `80 초과~100%`의 다섯 단계 색으로 구분한다.
- 공개 핏팅은 로그인 없이 조회·적용할 수 있다. 공개 저장·삭제, 좋아요와 내가 올린 핏팅 탭은 Google 로그인이 필요하다.
- 로그인 후 최초 프로필이 없으면 선택형 닉네임 안내를 한 번 표시한다. 최초 안내에는 나중에 프로필 메뉴에서 다시 설정할 수 있다는 문구와 `나중에 설정` 동작을 제공한다. 계정 메뉴에서 여는 설정·변경 팝업은 헤더의 닫기 버튼만 사용하고 별도 취소 버튼은 표시하지 않는다. 미설정 사용자는 공개 작성자명에서만 `Pilot`으로 표시한다. 계정 메뉴에서는 로그인 사용자에게 닉네임 설정 또는 변경 기능을 제공한다.
- 닉네임은 앞뒤 공백을 제거한 2~20자의 영문·숫자만 허용하고 대소문자를 구분하지 않고 중복 처리한다. `Pilot`은 미설정 표시용 예약어이므로 등록할 수 없다. 표시 닉네임과 소문자 예약 키는 `users/{uid}`와 `nicknames/{nicknameKey}`를 연결한 단일 Firestore 트랜잭션으로 설정한다. 변경할 때는 새 예약 생성, 프로필 변경, 기존 예약 삭제를 같은 트랜잭션에서 처리해 이전 닉네임을 다시 사용할 수 있게 한다.
- 공개 핏팅 소유권과 모든 보안 검사는 계속 Firebase UID를 사용한다. 닉네임은 핏팅 문서에 복제하지 않고 공개 목록에서 중복 UID를 제거해 아직 캐시되지 않은 `users/{uid}`만 단건 조회한다. 선택 닉네임과 `Pilot` 표시 캐시는 모두 60초 뒤 프로필 원본과 함께 무효화하고 다시 조회해 다른 탭에서 설정·변경한 닉네임을 반영한다. 프로필 조회 실패나 닉네임 없음은 `Pilot`로 표시하며 Firebase 프로필 기능 실패가 로그인이나 로컬 기능을 막지 않게 한다.
- 공개·내 게시물의 목록 카드와 상세 및 적용한 공개 핏팅 출처 패널에는 선택 닉네임 또는 `Pilot`을 작성자로 표시한다. Google 표시 이름·이메일·프로필 사진은 작성자 정보로 사용하지 않는다.
- 로그인 후 상단 계정 메뉴 버튼의 고정 표시는 `프로필`로 하며, 실제 선택 닉네임 또는 미설정 상태는 펼친 계정 메뉴 안에서 표시한다.
- 공개 및 내가 올린 핏팅의 상세 하단에는 `URL로 공유하기` 버튼을 표시하고 로컬 핏팅에는 표시하지 않는다. 버튼을 누르면 EXPORT와 같은 별도 팝업에 현재 언어와 공개 문서 ID를 <code>fitting</code> 쿼리로만 담은 공유 URL을 표시하며 로드아웃 코드는 포함하지 않는다. URL 입력은 팝업이 열릴 때 전체 선택하고 `URL 복사` 버튼은 Clipboard API를 사용하되 실패하면 선택 영역 복사로 대체한다.
- <code>fitting</code> URL로 진입하면 Firebase와 앱 데이터 준비를 기다린 뒤 유효한 문서 ID를 <code>fittings/{id}</code>에서 단건 조회한다. PC는 기존 공개 핏팅 적용·작성자·좋아요 경로를 재사용한다. 모바일은 로그인·공개 출처 UI를 만들지 않고 스키마와 `loadoutCode`만 검증해 공용 IMPORT 경로로 적용한 뒤 URL을 `loadout` 코드 주소로 교체한다. 목록을 열거나 전체 목록을 훑지 않으며 PC는 작성자 프로필이 캐시에 없을 때 <code>users/{uid}</code>를 한 번 추가 조회하고, 로그인 상태일 때만 해당 사용자의 좋아요 문서를 추가 조회한다. ID는 1~128자이고 슬래시를 허용하지 않으며, 없거나 잘못된 문서는 앱 전체를 중단하지 않고 상태 안내만 표시한다.
- 공개 핏팅 URL 상태는 일반 로드아웃·멕·주 탭보다 우선한다. 공유 링크로 최초 적용할 때는 현재 히스토리를 교체하고, 브라우저 상세에서 적용할 때는 새 히스토리를 추가한다. 이후 일반 멕·주 탭·로드아웃 탐색에서는 이전 <code>fitting</code> 쿼리를 제거한다.
- 공유 핏팅 출처 상태는 <code>fitting={문서 ID}</code> URL로 직접 들어오거나 핏팅 브라우저의 공개·내가 올린 핏팅 또는 추천 핏팅을 적용할 때 만든다. 모든 공개 문서 진입 경로에서 공유자·좋아요 버튼, 초록색 공유 탭과 원상복귀 동작을 제공한다. 로컬에서 적용한 빌드는 일반 피팅으로 취급한다. 추천 적용 시 작성자와 사용자 좋아요 상태를 보충하며, 추천 목록 표시만으로는 해당 메타데이터를 조회하지 않는다.
- 데스크톱 멕랩의 현재 피팅은 RIGHT ARM 아래에 같은 `mechId`의 공개 핏팅을 좋아요 수 내림차순으로 최대 3개 추천한다. 공유 문서 피팅에서는 공유자·좋아요 버튼이 있는 공유 정보 아래에 추천 목록을 표시한다. 카드에는 제목, 좋아요 수, 자동 태그와 `적용` 버튼을 표시한다. `적용`은 장착 무기와 상세 정보만 보여주는 간이 팝업을 열고, 팝업 작업은 헤더 X와 하단 `이 핏팅 적용`만 제공한다. 장착 무기와 상세 정보는 통합 핏팅 브라우저와 같은 렌더 함수를 사용한다.
- 개인설정의 `추천 핏팅 보이기`는 기본 ON이며 브라우저 로컬 저장소에 보존한다. OFF이거나 모바일이거나 멕랩의 실제 피팅 화면이 아니거나 <code>fitting</code> 문서를 불러오는 중이면 추천 영역과 추천 조회를 사용하지 않는다. 잘못되었거나 삭제된 공유 문서의 조회가 끝나 공유 피팅이 적용되지 않았으면 추천을 다시 허용한다.
- 추천 조회는 현재 `mechId`와 `likeCount` 내림차순 복합 인덱스를 사용한다. 멕별 결과는 브라우저 로컬 날짜를 키로 로컬 저장소에 보존하고 진행 중 요청은 메모리에서 중복 제거한다. 같은 날짜에는 페이지를 다시 열어도 캐시된 결과와 빈 결과를 재사용하며, 날짜가 바뀐 뒤 해당 멕을 처음 볼 때 최대 3개 문서만 다시 읽는다. 빈 원격 결과는 Firestore 최소 1회 읽기로 과금될 수 있다. 작성자 프로필, 사용자별 좋아요 문서와 실시간 리스너는 추천용으로 조회하지 않고, 조회 실패는 캐시하지 않아 다음 진입에서 다시 시도한다. 공개 핏팅을 게시·삭제하거나 좋아요를 변경하면 해당 멕의 메모리·로컬 캐시를 무효화하고 현재 멕일 때 같은 최대 3개 조회를 다시 실행한다.
- 기존 로컬 저장·불러오기 기능은 별도 버튼으로 중복 노출하지 않는다. `핏팅 브라우저 / 저장하기` 드롭다운의 브라우저와 저장하기 메뉴에서 로컬 탭과 통합 저장 대화상자를 제공한다.
- 저장 대화상자의 기본 위치는 로그인 상태면 공개, 비로그인 상태면 내 PC다. 공개·내 PC는 세로형 소형 토글로 표시한다. 제목만 필수이며 앞뒤 공백을 제거한 1~20자의 영문자·숫자·ASCII 특수문자만 허용하고 한글 등 비ASCII 문자는 저장하지 않는다. 대소문자와 관계없이 `https`가 들어간 제목도 광고 링크 방지를 위해 저장하지 않는다. 설명은 공개·로컬 저장 데이터에 기록하지 않는다.
- 목록 카드와 상세 헤더에서는 멕 이름, 제목 순서로 표시하되 멕 이름은 태그, 제목은 기본 텍스트 형태를 사용한다. 목록 카드의 좋아요 집계는 메크 썸네일 아래에, 작성자·날짜는 본문 하단 왼쪽에, 장착 무기 하드포인트는 오른쪽 아래에 둔다. 자동 태그는 대표 무장과 독립적으로 줄바꿈한다.
- 목록 카드의 대표 무장은 별도 `대표무기` 라벨 없이 무기 칩만 표시한다. 실제 장착 무기를 장비 ID별로 합산하고 `장비 중량 × 장착 수량`의 총 톤수 내림차순으로 정렬해 최대 4종만 표시한다. 동률은 로드아웃에서 처음 집계된 순서를 유지한다. 공격 무장과 AMS를 모두 후보로 사용하고, 4종을 초과해도 `[+]` 같은 추가 표시는 만들지 않는다. 표시 칩은 수량과 아이콘 없이 원본 장비 표시명만 사용한다.
- 목록 카드의 E·M·B·AMS 배지는 공용 하드포인트 배지 순서·마크업·색을 사용한다. 기체의 최대 하드포인트가 아니라 피팅에 실제 장착된 무기를 명시적 장비 하드포인트 타입으로 분류한 개수이며 값이 0인 타입은 숨긴다. 대표 무장과 상세 장착 무기 이름도 같은 타입 색을 사용한다.
- 상세 헤더는 왼쪽에 제목·자동 태그·작성자·업데이트 날짜를, 오른쪽에 여유 있는 크기의 좋아요 컨트롤을 둔다. 공개 탭은 로그인 여부와 관계없이 같은 위치를 유지하되 로그인한 사용자만 실행할 수 있고, 내가 올린 핏팅은 읽기 전용 집계로 표시하며 로컬 핏팅은 작성자와 좋아요를 숨긴다. 상세 좋아요는 화면에 아이콘과 집계 수만 표시하고 `좋아요`·`좋아요 취소` 동작명은 접근 가능한 이름과 툴팁으로 제공하며, 하단 작업 영역에는 중복 표시하지 않는다. 적용한 공개 핏팅 출처의 좋아요 버튼은 기존처럼 집계 숫자 없이 아이콘만 표시한다.
- 자동 태그는 로드아웃을 현재 공통 계산 경로로 분석해 브라우저 메모리에서 만들며 Firestore나 로컬 레코드에 저장하지 않는다. 무장 기반 태그의 정확한 범주·비율·최솟값은 `무기 관련 에이전트.md`를 따른다. 방어 태그는 아머 100%인 `풀아머`와 아머 65% 이하인 `유리장갑`을 사용하고, 현재 HSL을 반영한 고스트 힛이 실제 발생하는 로드아웃에는 `고스트 힛`을 표시한다.
- 검색은 핏팅 제목만 대상으로 한다. 브라우저 제목 옆의 멕 선택 버튼은 최상단 전체와 공용 소형 멕 목록 형태의 기종·개별 멕 필터를 제공한다. 기종 행의 `+`·`-` 영역만 배리언트 목록을 확장·축소하고, 행의 나머지 영역은 해당 기종 전체를 필터로 선택한다. 전체도 개별 멕과 같은 슬롯 행으로 표시하고 체급 구분과 기종별 배리언트 수는 표시하지 않는다. 기종 확장·축소 후에도 목록 스크롤 위치를 유지한다. 핏팅 화면에서 브라우저를 열면 현재 탭의 멕만, 멕 전체 화면의 IMPORT 왼쪽 직행 버튼에서 열면 전체를 기본 필터로 사용한다.
- 원격 목록은 전체, 사용자가 고른 단일 `mechId`, 또는 원본 멕 데이터의 `chassis`를 저장한 단일 `chassisKey` 조건으로 조회하고 사용자가 선택한 최신순 또는 좋아요순을 유지한다. 한 번에 최대 100개를 미리 읽고 다음 묶음 존재 여부를 확인하는 문서 1개를 추가 조회한다. 표시 페이지는 25개 단위이며 다음 묶음을 읽을 때는 실제 보관한 100번째 문서를 커서로 사용한다.
- 한 계정이 올릴 수 있는 공개 핏팅은 전체 멕을 합쳐 최대 100개다. 현재 게시 수와 한도는 `내가 올린 핏팅` 탭에 표시하지 않는다. 제한은 클라이언트 목록 길이가 아니라 사용자별 Firestore 카운터를 공개 핏팅 생성·삭제와 같은 트랜잭션에서 증감해 적용한다.
- `내가 올린 핏팅` 탭의 상세 하단에는 좋아요 실행 버튼을 표시하지 않는다. 최신순·좋아요순 정렬은 네이티브 셀렉트 대신 기존 핏팅 브라우저 액션과 같은 외곽선·화살표·팝오버 메뉴 규약을 사용한다.
- Firebase 초기화나 원격 목록 조회가 실패해도 브라우저의 공개·로컬·내 업로드 탭과 로컬 저장소 기능은 유지한다. 실패한 원격 탭의 목록 영역에만 현재 불러올 수 없다는 상태를 표시하며, 로그인 요구 상태도 전체 브라우저를 대체하지 않고 해당 탭 안에 표시한다.
- `fitting` 문서 URL, 브라우저의 공개·내 게시물 또는 추천에서 적용한 공유 핏팅 탭은 초록색으로 표시하되 활성 탭과 비활성 탭의 초록색 강도를 구분하고 RIGHT ARM 아래에 제목, 좋아요 여부를 나타내는 버튼과 불러온 공유 핏팅 자체를 다시 적용하는 원상복귀 동작을 표시한다. 출처 패널에는 좋아요 수를 별도로 표시하지 않는다. 원상복귀는 현재 피팅이 불러온 공개 원본과 달라졌을 때만 활성화한다. 다른 탭을 새로 만들거나 전환해도 기존 공유 탭의 출처 상태를 유지하며, 해당 탭 자체를 다른 멕이나 일반 피팅으로 교체하거나 저장할 때만 끝낸다. 브라우저 상세의 삭제·공유·적용 작업은 하단에 고정해 본문 스크롤과 무관하게 항상 보이게 하고 좋아요는 헤더에 유지한다.
- 저장 완료나 인증 상태 안내는 표시 후 3초가 지나면 자동으로 숨긴다.

## 4. 컴포넌트·슬롯 표시 규격

### 컴포넌트 배치

- 컴포넌트는 게임 배치와 가까운 5열 구조로 표시한다: 오른팔, 오른쪽 몸통·다리, 머리·중앙 몸통, 왼쪽 몸통·다리, 왼팔.
- 후방 장갑 의사 컴포넌트는 독립 슬롯 카드로 표시하지 않고 대응 몸통의 REAR 장갑에 반영한다.

### 컴포넌트 카드

- 제목, 전·후면 장갑 배분과 한도, 스트럭쳐, 남은 하드포인트, 경고, 크리티컬 슬롯을 표시한다.
- 모바일 컴포넌트 카드는 `ARMOR` 오른쪽에 실제 할당 포인트의 현재/기본 최대값을 표시한다. 전·후면 입력도 실제 할당 포인트를 사용하며 쿼크·스킬 증가량과 그 분해 표시는 숨긴다.
- 옴니포드가 장착된 부위는 포드 세트와 포드 하드포인트를 구분해 표시한다.
- 고정 장비, 내장 장비, 업그레이드 슬롯, 장착 아이템과 빈 슬롯을 모두 슬롯 사용량에 반영한다.
- 엔진이 제거된 배틀멕의 중앙 몸통에는 추출된 STD 엔진의 명시적 `stats.slots`와 같은 6칸을 `엔진 공간`이라는 반투명 전용 영역으로 표시하며 별도의 `E` 마크는 표시하지 않는다. 이 공간은 이미 할당된 현재 사용 슬롯으로 계산해 일반 장비와 구조·장갑 업그레이드가 사용할 수 없고, 엔진만 장착할 수 있다. 엔진 장착 시 예약 공간을 엔진의 실제 슬롯으로 대체하며 고정 엔진을 사용하는 옴니멕에는 빈 엔진 공간을 추가하지 않는다.
- 옵니포드 멕의 고정 엔진 내부에 들어간 히트싱크는 고정 장비로 표시하고 제거·복제·이동할 수 없게 한다. 엔진의 비어 있는 추가 히트싱크 슬롯은 표시하지 않으며 새 히트싱크를 넣을 수도 없다.
- 센터 고정 옵니포드에서도 하드포인트 태그와 수량은 에너지·미사일·발리스틱 등 공용 타입 색상을 유지한다. 회색 고정 표시는 옵니포드 고정 엔진 히트싱크에만 적용한다.
- 슬롯 또는 하드포인트 한도를 넘거나 데이터가 누락되면 해당 컴포넌트에 경고를 표시한다.

### 하드포인트 계산

- ECM 태그와 장착 위치는 현재 섀시 또는 선택한 옴니포드 컴포넌트의 명시적 `CanEquipECM`을 사용한다. STOCK LOADOUT의 ECM 장착 여부로 능력이나 위치를 추론하지 않는다.
- 빌드 컴포넌트에 옴니포드가 있으면 그 포드의 하드포인트를 사용한다. 포드 하드포인트가 비어 있어도 섀시 기본 하드포인트로 대체하지 않는다.
- 옴니포드가 없는 컴포넌트만 섀시 기본 하드포인트를 사용한다.
- 하드포인트 용량은 `hardpoint.weapon_slots`를 사용하며 원본 매핑에 값이 없을 때만 1을 대체값으로 사용한다.
- 기본 로드아웃에 장착된 무기 수로 최대 하드포인트를 추론하지 않는다.
- 멕 목록과 통계는 공통 하드포인트·슬롯 배지 렌더링을 사용한다. 옵니포드 멕의 멕 목록 하드포인트는 기본 로드아웃에 장착된 옵니포드를 적용한 유효 컴포넌트 정의에서 집계하며, 모바일도 PC 작은 멕 리스트가 생성한 같은 배지 HTML을 사용한다.

### 장착 아이템과 장비 목록

- 장착 아이템은 이름, 슬롯 점유, 무게와 제거 동작을 제공하고 원본 장비 툴팁과 연결한다.
- 데이터에서 장비를 찾지 못하면 누락 ID를 숨기지 않고 경고와 제거 동작을 제공한다.
- 장비 목록은 항목명, 슬롯, 무게와 장비 종류에 필요한 배지를 표시한다.
- 모바일 빈 슬롯의 무기 목록은 해당 부위에 남은 하드포인트 무기군별로 구분하고, 그룹 제목과 각 무기 행에 에너지·미사일·발리스틱·AMS의 공용 카테고리 색을 적용한다.
- 옵니포드 장비 목록의 타입별 하드포인트 수가 0이면 타입 고유색은 유지하면서 명도만 낮춰 표시한다.
- 멕랩 장비 표시 가능 여부는 `무기 관련 에이전트.md`의 필터 규칙을 따른다.

## 5. 수치 표시 규격

- 표시 숫자는 의미 없는 소수점 0을 줄이고 좁은 비교 셀에서는 필요한 정밀도를 유지한다.
- 엔진 표시는 장착 엔진의 `stats.rating`을 사용하며 선택 멕의 허용 범위를 벗어나면 경고한다.
- 빌드 톤수, 슬롯, 열, 탄약과 무장 수치는 공통 빌드 계산 결과를 사용하고 표시용으로 다시 계산하지 않는다.
- 무기 직격·스플래시·연사·사거리·알파 데미지 규칙은 `무기 관련 에이전트.md`를 따른다.
- 쿼크가 적용된 최종값과 기여도 색상은 `AGENTS.md`의 Quirk Parsing Rules를 따른다.

## 6. 변경 체크리스트

- 새 UI 수치를 추가할 때 정보, 비교, 통계, 멕랩 중 공유해야 할 범위를 정하고 같은 계산 경로를 사용한다.
- 멕 목록이나 통계 행을 바꿀 때 체급, 하드포인트, JJ, MASC 배지가 공통 렌더링을 유지하는지 확인한다.
- 슬롯이나 하드포인트 계산을 바꿀 때 일반 멕과 옴니멕, 컴포넌트 카드, 장비 필터, 목록과 통계를 함께 검토한다.
- 사용자 텍스트를 추가할 때 한국어·영어 번역과 `data-i18n` 계열 연결을 함께 추가한다.
- 구현 동작을 바꾸면 `위키 관리 지침.md`에 따라 구현 위키 갱신 여부를 판단한다.
- 기존 작업자의 수정이 있는 파일은 되돌리지 않고 필요한 부분만 좁게 수정한다.
