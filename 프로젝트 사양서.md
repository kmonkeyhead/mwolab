# MwoLab 프로젝트 사양서

이 문서는 MwoLab이 무엇을 제공하고 누구를 위해 존재하는지 정의하는 제품 사양의 기준이다.
구현 방법은 `AGENTS.md`가 연결하는 규칙 문서에, 현재 코드의 상세 동작은
`wiki/mwolab-implementation-wiki.html`에 기록한다.

- 서비스명: MwoLab
- 배포 주소: https://kmonkeyhead.github.io/mwolab/
- 기준 게임: MechWarrior Online (PGI)

## 1. 목적과 제품 원칙

### 1.1 해결하려는 문제

MechWarrior Online은 많은 멕·장비·옴니포드를 제공하지만 게임 클라이언트 안에서는
멕을 가로질러 탐색·비교하거나 빌드를 세밀하게 설계하기 어렵다.
MwoLab은 게임 밖에서 멕을 찾고, 비교하고, 빌드를 설계하는 도구다.

### 1.2 제품 원칙

1. **게임 원본 데이터를 기준으로 한다.**
   멕·장비·로드아웃·옴니포드·스킬의 원본 수치는 로컬에 설치된 MWO 데이터에서 추출한다.
   고정 공식이나 데이터 해석 규칙은 해당 규칙 문서에 정의하고, 구현 위키는 그 규칙을 복사하지 않고 적용 코드 위치만 연결한다.
2. **자체 서버 없이 정적 브라우저 앱으로 동작한다.**
   핵심 멕랩 계산과 로컬 기능은 브라우저에서 수행하고 `public/`만 정적 호스팅해 동작해야 한다.
   공개 핏팅 계정·저장·좋아요 기능에 한해 Firebase Authentication과 Cloud Firestore를
   관리형 백엔드로 사용할 수 있으며, 별도의 자체 서버나 서버 런타임은 운영하지 않는다.
3. **도구형 화면을 유지한다.**
   장식보다 스캔·비교·반복 사용을 우선하고, 정보의 깊이를 위해 필요한 밀도를 유지한다.
4. **같은 계산은 같은 결과를 낸다.**
   같은 입력에 대해 정보, 비교, 통계, 멕랩의 공통 수치가 서로 달라지지 않아야 한다.

### 1.3 제품 범위

다음 기능은 범위 안이다.

- 멕 탐색·필터·비교와 상세 스펙 조회
- 멕 빌드 편집, 로컬 저장, MWO 로드아웃 코드 입출력
- 서버를 사용하지 않고 로드아웃 코드를 URL-safe 가역 압축해 URL 쿼리에 담는 빌드 공유와 기존 비압축 URL 읽기 호환
- 공개·로컬·내 게시물을 한 브라우저에서 관리하고, Google 계정으로 공개 게시·삭제와 좋아요를 처리하는 기능
- 공개 게시물의 문서 ID를 담은 URL로 특정 핏팅을 바로 공유·적용하는 기능
- 로그인 사용자가 선택형 고유 닉네임을 설정·변경하고 공개 핏팅 작성자로 표시하는 기능
- 장비 정보 조회, 무기 성능 분석, DPS 시뮬레이션
- 전체 멕을 대상으로 한 내구도·기동성·쿼크 통계
- 한국어와 영어 UI
- 휴대폰·태블릿에서는 별도 모바일 멕랩으로 자동 진입하며 멕 선택, 피팅 편집, 업그레이드와 MWO 코드 입출력을 제공

다음 기능은 범위 밖이다.

- 자체 서버 API와 자체 계정·비밀번호 관리 시스템
- 비공개 클라우드 핏팅 동기화 및 Firebase 이외의 서버 측 빌드 저장
- 드랍십 또는 팀 구성 기능
- 게임 클라이언트와의 실시간 연동
- 사용자에게 제공하는 위키 기능

`wiki/mwolab-implementation-wiki.html`은 제품 기능이 아니라 개발자가 현재 구현을 확인하는 내부 문서다.

### 1.4 데이터 흐름

MWO 설치 데이터에서 브라우저용 JSON을 추출하고 앱이 이를 읽어 계산한다.
데이터 파일 종류, 현재 개수, 추출 절차와 검증 규칙은 `public/data/index.json`과
`JSON 추출 지침.md`를 기준으로 한다.

## 2. 제공 기능

### 2.1 공통 탐색과 상태

- 멕 브라우저에서 기종과 배리언트를 검색·정렬·필터링한다.
- URL로 탭, 선택 멕, 언어, 공유 로드아웃 또는 공개 핏팅 문서 상태를 전달할 수 있다.
- 사용자 빌드와 표시 설정은 필요한 범위에서 브라우저에 저장한다.
- 데스크톱 개인설정에서 새 멕의 초기 장비 상태와 부위별 아머 프리셋을 저장한다.
- 공개 핏팅 기능에서는 Google 로그인 상태와 게시물 소유 계정을 구분한다.
- 공개 핏팅 소유권은 Firebase UID로 유지하고 선택 닉네임은 표시 용도로만 사용한다.
- 한국어를 기본으로 하며 영어 UI를 함께 제공한다.

### 2.2 메인 작업 영역

- **멕랩**: 여러 멕 핏팅을 탭으로 함께 열어 두고 장갑·장비·옴니포드·업그레이드·스킬을 편집하며 빌드 상태를 확인한다.
- **무장 정보**: 특정 멕과 무관하게 무기, 모듈, 고스트 힛 정보를 조회한다.
- **정보**: 선택한 멕의 내구도, 기동성, 엔진, 쿼크를 확인한다.
- **비교하기**: 여러 멕을 나란히 놓고 공통 수치와 차이를 비교한다.
- **통계**: 전체 멕을 내구도, 기동성, 쿼크 기준으로 순위화하고 상세 정보를 확인한다.

탭 순서, 레이아웃, 카드와 하위 메뉴의 표시 규격은 `프로젝트 규약 에이전트.md`에 둔다.

### 2.3 분석과 공유

- 빌드의 무장 성능을 거리와 사격 조건에 따라 분석한다.
- DPS 시뮬레이션에서 무기 그룹, 거리, 열 환경과 사격 시나리오를 조절한다.
- 통합 핏팅 브라우저에서 이 PC의 로컬 빌드와 공개·내 게시물을 구분해 조회·적용·관리한다.
- 멕랩에서 현재 멕과 같은 공개 핏팅 중 좋아요 상위 항목을 최대 3개 추천하고 간이 정보 확인 후 적용할 수 있다.
- 로컬 빌드를 저장·관리하고 MWO 로드아웃 코드를 입출력한다.
- 신규 공유는 로드아웃 코드를 URL-safe 가역 압축한 URL로 만들고 서버 저장 없이 빌드를 공유한다. 기존 전체 로드아웃 코드를 담은 URL도 계속 읽는다.
- 공개 핏팅의 문서 ID를 포함한 URL을 만들어 게시된 핏팅을 직접 공유하고, 방문 시 해당 문서만 조회해 적용한다.
- 로그인 사용자는 현재 핏팅을 공개 게시·삭제할 수 있고, 방문자는 로그인 없이 게시된 핏팅을 조회·적용할 수 있다.
- 로그인 사용자는 공개 핏팅마다 계정당 한 번 좋아요를 표시하거나 취소할 수 있다.
- 닉네임을 설정하지 않은 게시자는 `Pilot`으로 표시하며 닉네임 설정은 공개 핏팅 사용의 필수 조건이 아니다.

무기 계산 규칙은 `무기 관련 에이전트.md`, 시뮬레이션 공통 규칙은 `AGENTS.md`를 따른다.

## 3. 대상 사용자

### 3.1 1차 대상

한국어권 MechWarrior Online 플레이어가 1차 대상이다. 게임 내 영어 원본명과 대조할 수 있도록
장비명, 스탯 키, 진영명 등 원본 데이터의 고유 표기는 영어를 유지한다.

### 3.2 숙련도 범위

- 입문 사용자는 멕 탐색과 정보 조회로 멕의 성격을 파악한다.
- 중급 사용자는 비교와 통계로 사용할 멕을 선택한다.
- 상급 사용자는 멕랩, 무장 분석, 시뮬레이션과 상세 쿼크 지표로 빌드를 설계한다.

기능의 깊이는 상급 사용자 쪽에 무게를 두되, 공통 탐색과 기본 정보는 입문 사용자도 접근할 수 있어야 한다.

### 3.3 2차 대상

영어권 MWO 플레이어가 2차 대상이다. 사용자에게 보이는 새 텍스트는 한국어와 영어를 함께 제공해야 한다.
구체적인 다국어 구현 규칙은 `프로젝트 규약 에이전트.md`에 둔다.

### 3.4 모바일 사용자

휴대폰·태블릿 사용자는 별도 경량 멕랩의 대상이다. 모바일 버전은 멕 선택과 피팅 편집에 집중하고
정보·비교·통계·무장 정보·시뮬레이션·스킬·로그인·공개 및 로컬 핏팅 브라우저는 제공하지 않는다.
휴대폰·태블릿에는 PC 보기 선택을 제공하지 않으며 기본 주소로 다시 들어와도 항상 모바일 버전으로 진입한다.

### 3.5 비대상

- MWO를 플레이하지 않아 게임 용어 설명이 필요한 일반 방문자

## 4. 최상위 제약과 문서 체계

### 4.1 기술 제약

- `public/` 하나로 배포 가능한 정적 앱 구조를 유지한다.
- 서버 런타임, 번들러, 프레임워크를 제품 실행의 전제로 추가하지 않는다.
- 공개 핏팅 기능의 관리형 백엔드는 Firebase Authentication과 Cloud Firestore로 제한한다.
- Firebase 프로젝트는 결제 계정을 연결하지 않은 Spark 무료 요금제를 유지하며,
  무료 할당량 초과 시 유료 전환이나 추가 과금 대신 해당 기능의 제한 또는 중단을 허용한다.
- Firebase 장애나 할당량 제한이 기존 멕랩 계산, 로컬 저장, 로드아웃 코드 입출력과
  URL 기반 공유 기능을 불필요하게 중단시키지 않도록 기능 경계를 유지한다.
- 게임 데이터와 공통 계산을 탭마다 별도로 복제하지 않는다.

### 4.2 문서 소유권

| 문서 | 담당 범위 |
|---|---|
| `프로젝트 사양서.md` | 제품 목적, 제공 기능, 대상 사용자, 제품 범위, 최상위 제약 |
| `AGENTS.md` | 작업 규칙의 진입점과 여러 도메인에 걸친 공통 구현 규칙 |
| `프로젝트 규약 에이전트.md` | UI 원칙, 탭 표시 규격, 슬롯·하드포인트 표시 규격 |
| `무기 관련 에이전트.md` | 무기 계산과 툴팁, 멕랩 장비 필터 |
| `쿼크 관련 에이전트.MD` | 정보와 통계의 쿼크 서머리 명칭 대응 |
| `JSON 추출 지침.md` | 게임 데이터 추출과 생성 JSON 변경 |
| `위키 관리 지침.md` | 구현 위키의 확인과 갱신 절차 |
| `wiki/mwolab-implementation-wiki.html` | 현재 구현의 상세 동작과 코드 해석 |
| `CLAUDE.md` | Claude가 위 문서 체계로 진입하기 위한 안내 |

한 주제가 여러 계층에 걸치면 사양서에는 제품이 제공해야 할 능력만, 규칙 문서에는 재사용할 구현 요구만,
구현 위키에는 현재 코드의 구체적 동작만 기록한다. 같은 규칙 문장을 여러 문서에 복사하지 않는다.
